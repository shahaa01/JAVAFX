import java.util.Stack;

public class sjdc {
	private Stack<int[]> moveStack = new Stack<>();
	private Stack<String> cardStack = new Stack<>();
	//constructor
	
	//drag detection on cardPane 
	
	//drag release detection
	//srcPane = (cardPane)e.getGestureSource();
	//dest = e.getSource();
	//srcRow = grid.getRowIndex(df);
	
	//check if its valid or not
	/**
	 * If valid
	 * set the src to dest
	 * and set src to empty
	 * 
	 * and have that moves into moves stack
	 * and have that string of
	 */
}
